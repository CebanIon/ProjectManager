@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    Layout = null;
}
<div class="card p-3 px-4 m-2 d-inline-block align-top border border-3 rounded shadow">
    <div class="card-body">
        <h5 class="card-title text-decoration-underline">Projects:</h5>
        @if (ViewBag.AllProjects != null)
        {
            @foreach (var project in ViewBag.AllProjects)
            {
                <p class="w-100">@project.Name       @project.State</p>
            }
        }
        else
        {
            <p class="h6">No projects to show</p>
        }
    </div>
</div>
